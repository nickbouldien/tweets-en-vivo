// app / environment config

// TODO - first check env variables for port/path values
export const port = 5000;
export const path = "/ws";
export const websocketUrl = `ws://localhost:${port}${path}`;
